\begin{answer}
We apply the degree-3 feature map $\phi(x)=[1,x,x^2,x^3]^T$ to all training inputs and solve the normal equations $\hat{X}^T\hat{X}\theta = \hat{X}^Ty$ using \texttt{np.linalg.solve}. See \texttt{src/featuremaps/featuremap.py}. The scatter plot of training data with the fitted degree-3 curve is shown below.
\end{answer}

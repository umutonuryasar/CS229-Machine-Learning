\begin{answer}
On Dataset 1, both logistic regression and GDA find very similar decision boundaries and achieve comparable accuracy. This is expected: Dataset 1 is well-separated and approximately Gaussian, so GDA's generative model assumptions are satisfied, giving it roughly the same performance as the discriminative logistic regression. Both decision boundaries are nearly identical.
\end{answer}

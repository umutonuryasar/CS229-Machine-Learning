\begin{answer}
Dataset 2 has one class whose $x_1$ values appear to follow an exponential distribution (always positive, long right tail). Taking the log transform $x_1 \leftarrow \log(x_1)$ makes this class approximately Gaussian, satisfying GDA's assumptions. After replacing $x_1$ with $\log(x_1)$, GDA's performance on DS2 improves significantly and matches logistic regression.
\end{answer}

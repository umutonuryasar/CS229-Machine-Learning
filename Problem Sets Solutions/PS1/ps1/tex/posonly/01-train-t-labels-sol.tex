\begin{answer}
We train logistic regression on the true $t$-labels. This gives the best possible boundary since we have access to the ground-truth labels. See \texttt{src/posonly/posonly.py}. The plot of the test set with the decision boundary is included below.
\end{answer}

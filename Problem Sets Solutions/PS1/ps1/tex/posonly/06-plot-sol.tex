\begin{answer}
We estimate $\alpha$ by averaging $h(x^{(i)})$ over all $y=1$ examples in the validation set, then rescale predictions by $1/\alpha$ to recover $p(t=1|x)$. See \texttt{src/posonly/posonly.py}. The adjusted decision boundary (where the rescaled probability $= 0.5$, i.e., $p(y=1|x) = \alpha/2$) is plotted below and closely matches the boundary obtained from the true $t$-labels in part (a).
\end{answer}

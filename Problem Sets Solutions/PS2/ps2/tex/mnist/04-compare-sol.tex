\begin{answer}
\begin{center}
\begin{tabular}{lc}
\toprule
Model & Test Accuracy \\
\midrule
Without regularization & $\approx 0.9287$ \\
With regularization ($\lambda=0.0001$) & $\approx 0.9676$ \\
\bottomrule
\end{tabular}
\end{center}

The regularized model outperforms the non-regularized model on the test set because $L_2$ regularization prevents the weights from growing too large, reducing overfitting to the training distribution and yielding better generalization to unseen examples — exactly as expected from regularization theory.
\end{answer}


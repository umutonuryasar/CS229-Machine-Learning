\begin{answer}
See \texttt{src/perceptron/perceptron.py} for the implementation of \texttt{initial\_state}, \texttt{predict}, and \texttt{update\_state}.

The resulting decision boundary plots are included below (one per kernel). The plots are saved as \texttt{perceptron\_dot\_output.png}, \texttt{perceptron\_rbf\_output.png}, and \texttt{perceptron\_non\_psd\_output.png}.
\end{answer}

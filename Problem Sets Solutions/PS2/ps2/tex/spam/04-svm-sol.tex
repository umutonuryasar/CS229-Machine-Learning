\begin{answer}
See \texttt{src/spam/spam.py} for the implementation of \texttt{compute\_best\_svm\_radius}.

Searching over radii $\{0.01, 0.1, 1, 10\}$, the best SVM radius found by validation accuracy is $\boxed{0.1}$. With this radius, the SVM achieves an accuracy of \textbf{96.95\%} on the test set.
\end{answer}
